{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug SEO Agent with UV",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "args": ["--input", "data/", "--output", "output", "--batch", "1", "--max-concurrent", "1"],
      "console": "integratedTerminal"
      // "justMyCode": false
      // "env": {
      //   "PYTHONPATH": "${workspaceFolder}"
      // }
    }
  ]
}
